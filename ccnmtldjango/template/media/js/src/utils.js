define([
    'underscore'
], function(_) {
    var utils = {

        /**
         * This is just an example util that can be removed.
         */
        isEven: function(n) {
            return (n % 2) === 0;
        }

    };

    return utils;
});
